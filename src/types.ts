export type Project = {
  title: string;
  description: string;
  imageUrl: string;
  githubUrl: string;
  tags: string[];
};

export type TechStack = {
  category: string;
  skills: string[];
  icon: string;
};